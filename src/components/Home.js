const Home = () => {
  return <header>
      <title>N-Queens EA</title>
      <h1>N-Queens</h1>
      <h2>Solved by Evolutionary Algorithm</h2>

      <p>Use the "Options" panel to set the profile of the experiment.  Click run from that page to see all the results.  Select a result to see what it looks like on a chess board.</p>
  </header>;
};

export default Home;
